# Natural Language to SQL Query Converter

## 📌 Project Overview
The Natural Language to SQL Query Converter is a Java-based application that enables users to interact with relational databases using plain English queries.  
The system processes natural language input, identifies user intent, and dynamically generates accurate SQL queries, making database access easier for non-technical users.

---

## 🛠️ Technologies Used
- Java  
- Natural Language Processing (NLP)  
- SQL  
- MySQL  
- JDBC  

---

## ✨ Key Features
- Converts English language questions into executable SQL queries  
- Implements NLP preprocessing techniques such as:
  - Tokenization  
  - Keyword extraction  
  - Intent identification  
- Maps extracted entities to a relational database schema  
- Dynamically generates SQL queries based on user input  
- Executes queries and displays real-time results  
- Successfully processes **100+ sample natural language queries**

---

## 🧠 System Workflow
1. User enters a natural language query  
2. Input text is preprocessed using NLP techniques  
3. Keywords and intent are identified  
4. Relevant database tables and attributes are mapped  
5. SQL query is generated dynamically  
6. Query is executed on the database  
7. Results are displayed to the user  

---

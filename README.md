# NL-TO-SQL
The Natural Language to SQL Query Converter is a Java-based application that enables users to interact with relational databases using plain English queries. The system processes natural language input, identifies user intent, and dynamically generates accurate SQL queries, making database access easier for non-technical users.
